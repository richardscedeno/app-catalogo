import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-catalogos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
